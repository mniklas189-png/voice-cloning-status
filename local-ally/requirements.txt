# Pflicht: die Oberflaeche
slint>=1.9.0a1

# Mikrofonaufnahme (PortAudio). Ohne dieses Paket startet Local Ally,
# kann aber nicht zuhoeren.
sounddevice>=0.4.6

# Spracherkennung - mindestens eines von beiden wird gebraucht.
# Vosk ist die Vorgabe: klein, streamend, sehr schnell.
vosk>=0.3.45

# Optional, genauer, dafuer rechenintensiver:
# faster-whisper>=1.0.0

# Globale Tastenkuerzel (Stummschaltung, Push-to-Talk). Ohne dieses Paket
# laeuft Local Ally weiter - nur eben ohne systemweite Kuerzel.
pynput>=1.7.6
